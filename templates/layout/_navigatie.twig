{% block content %}

{% set mainNav = craft.navigate.raw('navigatie') %}

    <nav class="navigatie">
        <ul>
            {% for node in mainNav %}
                <li>
                    <a href="{{ node.url}}">{{ node.name}}</a> </li>
            {% endfor %}
        </ul>
    </nav>

{% endblock %}