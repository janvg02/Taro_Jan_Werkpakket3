{% set releaseQuery = craft.app.request.getParam('releaseYear') %}
{% set productionQuery = craft.app.request.getParam('productionCompany') %}

{% set relationParam = ['and'] %}

{% if releaseQuery %}
    {% set relationParam = relationParam|merge([{ targetElement: releaseQuery }]) %}
{% endif %}
{% if productionQuery %}
    {% set relationParam = relationParam|merge([{ targetElement: productionQuery }]) %}
{% endif %}

{% set entries = craft.entries.section('product') %}

{% if relationParam|length > 1 %}
    {% set entries = entries.relatedTo(relationParam) %}
{% endif %}

{% paginate entries.limit(6) as pageInfo, pageEntries %}

<section>
    <div class="page-container">

        <div class="filter-container">
            <div class="filter-section">
                <form method="get" id="filterForm">
                    <h2>Jaar van uitgave</h2>
                    {% for releaseYear in craft.entries.section('releaseYears') %}
                        <div class="filterItem">
                            <input class="filter-checkbox" type="radio" id="releaseYear{{ releaseYear.id }}" value="{{ releaseYear.id }}" name="releaseYear" {% if releaseQuery and releaseYear.id == releaseQuery %} checked {% endif %}>
                            <label class="filter-text" for="releaseYear{{ releaseYear.id }}">{{ releaseYear.title }}</label>
                        </div>
                    {% endfor %}
                    <h2>Bedrijf</h2>
                    {% for productionCompany in craft.entries.section('productionCompany') %}
                        <div class="filterItem">
                            <input type="radio" id="productionCompany{{ productionCompany.id }}" value="{{ productionCompany.id }}" name="productionCompany" {% if productionQuery and productionCompany.id == productionQuery %} checked {% endif %}>
                            <label for="productionCompany{{ productionCompany.id }}">{{ productionCompany.title }}</label>
                        </div>
                    {% endfor %}

                    <button type="button" id="resetButton" class="add-to-cart-button">Reset Filters</button>
                </form>
            </div>
        </div>

        <div class="product-container">
            {% if pageEntries | length %}
                <ul class="product-lijst">
                    {% for product in pageEntries %}
                        {% set image = product.productImage.one() %}
                        <li class="product-info">
                            <a href="{{ url('products/' ~ product.slug) }}">
                                <img src="{{ image.url }}" alt="{{ product.title }}" class="product-image">

                                <div class="product-details" id="details">
                                    <h1 class="product-title">{{ product.title }}</h1>
                                    <p class="productdescription">{{ product.productDescription }}</p>
                                    <p class="stock">{{ product.productStock }}</p>
                                    <p class="price">€ {{ product.productPrice }}</p>
                                    <button class="add-to-cart-button">Bekijk product</button>
                                </div>
                            </a>
                        </li>
                    {% endfor %}
                </ul>

                <div class="pagination">
                    {% for i in 1..pageInfo.totalPages %}
                        <button onclick="location.href='{{ pageInfo.getPageUrl(i) }}'" class="page-numbers{% if pageInfo.currentPage == i %} active{% endif %}">
                            {{ i }}
                        </button>
                    {% endfor %}
                </div>
            {% else %}
                <p>No products available.</p>
            {% endif %}
        </div>
    </div>
</section>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const filterForm = document.getElementById('filterForm');
        const resetButton = document.getElementById('resetButton');

        filterForm.addEventListener('change', function () {
            filterForm.submit();
        });

        resetButton.addEventListener('click', function () {
            // Reset all radio buttons
            const radioButtons = filterForm.querySelectorAll('input[type="radio"]');
            radioButtons.forEach(function (radio) {
                radio.checked = false;
            });

            // Submit the form to apply the reset
            filterForm.submit();
        });
    });
</script>
